name: memory_cards_examples
description: Reference examples for the Memory Cards Flutter course.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0

flutter:
  uses-material-design: true
